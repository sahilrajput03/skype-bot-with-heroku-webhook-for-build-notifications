// import {Api, connect} from 'skype-http';
import dotenv from 'dotenv';

// FYI: This library isn't working.
// TODO: Next time try: https://github.com/softwaregroup-bg/ut-port-skype

// Run this file via: nodemon `src/skype-test.ts` command.
// Source: https://github.com/ocilo/skype-http

// Load .env's variables
// dotenv.config();

// const username = process.env.EMAIL!;
// const password = process.env.PASSWORD!;

// async function run() {
// 	const api: Api = await connect({credentials: {username, password}});
// 	for (const contact of await api.getContacts()) {
// 		await api.sendMessage({textContent: 'Hello, World!'}, contact.mri);
// 	}
// }

// run();
